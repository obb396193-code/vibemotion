# OS / editor junk
.DS_Store
**/.DS_Store
Thumbs.db

# deps / build
node_modules/
out/
*.log
.env
.env.*

# rendered outputs (keep the repo light; re-render from source)
*.mp4
*.mov

# audio kept out of the public repo (licensing); supply your own. route-B uses no audio anyway.
*.mp3
*.wav
*.m4a
*.aac
